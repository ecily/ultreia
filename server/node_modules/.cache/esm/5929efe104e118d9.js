let User,Stripe;_04e‍.x([["createConnectAccount",()=>createConnectAccount]]);_04e‍.w("../models/user",[["default",["User"],function(v){User=v}]]);_04e‍.w("stripe",[["default",["Stripe"],function(v){Stripe=v}]]);

//import and execute right away
stripe(process.env.STRIPE_SECRET)

       const createConnectAccount = async (req, res) => {
    
    //find user in db
    const user = await User.findById(req.user._id).exec()
    _04e‍.g.console.log(user)
    //if no stripe id yet, create one
    const account = await stripe.accounts.create({
        type: 'express'
    })
    _04e‍.g.console.log('ACCOUNT ====>', account)
}